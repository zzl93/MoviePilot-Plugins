# 功能对照 – 重构 v2（更新于 2026-03-08）

| # | 重构前已完成功能 | 新仓库位置 | 当前实现现状 | 缺口 / 风险 | 建议下一步 |
|---|------------------|------------|---------------|-------------|-----------|
| 1 | 站点做种抓取入库：抓 NexusPHP `getusertorrentlistajax` / `torrents.php?type=seeding`，写入种子表+快照+PluginData 统计（旧版：`server/page_parser.py` + `server/sync_bonus_data.py`）。 | `MVC/services/site_service.py`, `MVC/mappers/site_seed_mapper.py`, `MVC/utils/page_parser.py`. | `site_service.sync_site_seeding_data` 已实现 HTTP 请求与 `SiteSeedMapper.batch_save_seeding_from_parser` 持久化，同时写 `site_info_{domain}`。 | `page_parser.parse_torrent_activity_nexusphp` 仍只解析名称/大小，`seed_time` 与 `bonus_per_hour` 固定为 0，`parse_bonus_params_nexusphp` 返回空 dict，导致 `SiteSeedSnapshot` 中关键字段缺失，PluginData 也缺少 bonus 参数。缺少单测，无法验证多页/字段解析。 | 迁移旧仓库 parser：补齐 bonus 参数、做种时长、权重/种子数解析，覆盖多页场景；引入 `temp/` 样本编写 `tests/parser` 单测；确认 `SiteSeedMapper` 可写入 bonus 参数并记录日志。 |
| 2 | 下载器抓取入库：按配置下载器拉取种子列表、写入 `DownloaderSeed` + `Snapshot`，保留已有 `site_seed_id`（旧版：`server/downloader_helper.py` + `server/utils.build_torrents_by_tracker_domain` 等）。 | `MVC/utils/downloader_fetcher.py`, `MVC/services/downloader_seed_service.py`, `MVC/mappers/downloader_seed_mapper.py`. | 提供 `fetch_downloader_torrents`、`batch_upsert_downloader_torrents` 等基础能力。 | `sync_downloader_seeds_from_api` 未在插件生命周期中被调用（`__init__.py`、`controller.py`、`bonus_service` 均未触发），导致下载器表无数据；缺少 tracker->site 映射逻辑（旧版依赖 `keyword_to_domain`/`build_torrents_by_tracker_domain`）。无验证脚本。 | 将下载器同步挂到站点刷新链路、手动刷新和定时任务；迁移旧版 tracker 解析/分组逻辑，补 `site_address_mappings` 过滤；提供 CLI/脚本以便验证拉取成功。 |
| 3 | `site_seed_id` 关联与保存：前端保存匹配时写入 downloader 表，后续刷新能显示 `matched`（旧版：`server/utils.match_torrent_core`、`server/seedinfo_oper.py`）。 | `MVC/mappers/downloader_seed_mapper.py`, `MVC/services/bonus_service.py` (`apply_save_data`, `get_bonus_seeding_data`), `MVC/controller.py` (`api_save_data`). | `/save_data` 已接到 `bonus_service.apply_save_data` 并调用 `DownloaderSeedMapper.upsert_downloader_seed` 写 `site_seed_id`。 | 缺少自动匹配算法：`bonus_service.build_seed_association_payload` 仅返回站点 rows 与下载器候选，但没有像旧版那样根据名称/大小自动打分；`list_downloader_candidates_for_site` 依赖 `site_address_mappings` 但没有 token 匹配。前端即便点“保存”也需要手工选择，且没有移除/解除逻辑的测试。 | 迁移旧版 `match_torrent_core`、分词与大小 tolerance 函数，生成 `suggested_match` 字段供前端勾选；在 `apply_save_data` 中处理 `site_seed_id` 为空时的解绑；为 mapper 增加单元测试验证 `site_seed_id` 持久化。 |
| 4 | 站点刷新触发链路：主项目 SiteRefreshed 事件或插件页面“刷新”按钮 → 拉站点、下载器、bonus 参数并更新 Page（旧版：`__init__.py` + `server/sync_bonus_data.py`）。 | `__init__.py`（事件注册 `_on_site_refreshed`）, `MVC/controller.py` (`on_site_refreshed`, `/bonus_data`, `/save_data`), `MVC/services/site_service.py`. | Event listener 已注册并会在 SiteRefreshed 时调用 `site_service.trigger_sync_on_site_refresh`，页面 API `/bonus_data` 能读 `SiteSeed` + 下载器候选。 | 事件链只触发站点抓取，未串联下载器同步、bonus 参数刷新或 PluginData 指标更新；`api_bonus_data` 也没在返回前调用任何同步逻辑，导致页面依赖“之前有人手动跑过脚本”；缺少“刷新”命令或任务记录。 | 在 `_on_site_refreshed` 和 `/bonus_data` 请求前增加 orchestrator：顺序执行站点抓取 → 下载器 `sync_downloader_seeds_from_api` → bonus 参数/PluginData 更新；补充日志与失败告警；记录验证脚本在 `project-work-logs`. |
| 5 | 时魔计算：解析 mybonus 参数（T0/N0/B0/L、单种奖励等），按大小/做种时长/权重计算 `bonus_per_hour` 并在前端展示总和（旧版：`server/page_parser.py` + `server/utils.calc_bonus_per_hour` 等）。 | `MVC/utils/page_parser.py` (`parse_bonus_params_nexusphp`), `MVC/utils/bonus.py` (`calc_bonus_per_hour`), `MVC/services/bonus_service.py:get_bonus_seeding_data`. | 目前 `parse_bonus_params_nexusphp` 返回 `None`，`calc_bonus_per_hour` 只是占位（仅使用 `factor`）；`get_bonus_seeding_data` 虽尝试在缺 bonus 时重新计算，但因缺参数始终为 0。 | 前端显示的 `bonus_per_hour`、`total_bonus_per_hour` 始终为 0，无法指导删种策略；也缺少 `seed_time` 数据来源导致 T/N 计算无从谈起。 | 迁移旧版 T/N/B/L 解析与 `calc_bonus_per_hour` 正式实现，引入 `parse_pubdate_weeks`、权重/seed_time 计算；在 `site_service.sync_site_seeding_data` 后写回 PluginData，并添加回归测试覆盖真实种子样本。 |

> 注：旧版代码位于 `D:/project/MoviePilot-Plugins/plugins.v2/ptbonuscalc/server/`，可直接参考 `page_parser.py`、`utils.py`、`sync_bonus_data.py` 等模块完成迁移。

